% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_ec.R
\name{build_ec}
\alias{build_ec}
\title{Build equivalence classes for bulk RNA-seq quantification}
\usage{
build_ec(
  input,
  keep_temp = FALSE,
  chunk_size = 5000000L,
  n_cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{input}{An \code{IsoEMInput} object from \code{\link{prepare_isoem}},
with \code{mode} set to \code{"bulk_single"} or \code{"bulk_multi"}.}

\item{keep_temp}{Logical. Keep the \code{temp/} directory after EC
construction (default FALSE).}

\item{chunk_size}{Integer. Rows per chunk when reading large files
(default 5e6).}

\item{n_cores}{Integer. Parallel workers for EC construction across
samples (default 1).}

\item{verbose}{Logical (default TRUE).}
}
\value{
An \code{IsoEMEC} object.
}
\description{
Reads IsoQuant counts, merges group (sample) information, and constructs
an \code{IsoEMEC} object ready for \code{\link{run_em}}.
}
\examples{
counts_f <- system.file("extdata", "toy_counts.tsv",  package = "IsoEM")
gtf_f    <- system.file("extdata", "toy.gtf",         package = "IsoEM")
input    <- prepare_isoem(counts_f, gtf_f,
  mode = "bulk_single", sample_id = "toy")
ec <- build_ec(input)
print(ec)
}
\seealso{
\code{\link{prepare_isoem}}, \code{\link{run_em}}
}
